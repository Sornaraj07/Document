sudo subscription-manager register --username=sornaraj07@gmail.com --password=Sorna@07
sudo subscription-manager register --username sornaraj07@gmail.com --auto-attach
sudo subscription-manager clean
sudo yum repolist


Install Ansible 
---------------

sudo yum update -y
sudo yum -y install python3.12
sudo yum -y install python3.12-pip
python3.12 -m pip install --user ansible
python3.12 -m pip install --user pywinrm
